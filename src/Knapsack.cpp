#include <iostream>
#include <vector>


std::vecotor<int> knapsack(std::vector<int> weight, std::vector<int> value, int maxWeight){
	std::vector<int> finalValueList;
	
	for(int i = 0; i < weight.size(); ++i){
		for(int j = 0; j < weight.size(); ++j){
			
		}
	}	
}
